const makeArrayDevidedByWeek = (flattenArray : Array<Boolean>) => {
  let monthArr = []
  let weekArr = []
  let falseCount = flattenArray.filter(item => !item).length
  console.log(falseCount)
  

  return 
}

export default makeArrayDevidedByWeek